---
import BaseLayout from '../layouts/BaseLayout.astro';
import Button from '../components/ui/Button.astro';
import Container from '../components/ui/Container.astro';
import Icon from '../components/ui/Icon.astro';
---

<BaseLayout title="Instruction · Awesome Copilot" description="Explore and install a GitHub Copilot instruction.">
  <section class="agent-detail" aria-labelledby="instruction-title">
    <Container>
      <nav class="breadcrumbs" aria-label="Breadcrumb">
        <a href="/">Home</a><span aria-hidden="true">/</span><a href="/instructions/">Instructions</a><span aria-hidden="true">/</span><span id="breadcrumb-current" aria-current="page">Loading instruction</span>
      </nav>

      <div class="detail-layout">
        <main class="detail-main">
          <header class="hero-panel pixel-panel">
            <p class="terminal-line" id="instruction-terminal-line">cat ./instructions/loading.instructions.md</p>
            <div class="title-stack">
              <span class="detail-badge detail-badge--blue" id="instruction-label">loading</span>
              <h1 id="instruction-title">Loading instruction</h1>
              <p id="instruction-description">Fetching instruction details on demand.</p>
            </div>
            <div class="hero-actions" aria-label="Primary actions">
              <Button href="#install" variant="primary"><Icon name="download" size={15} /> Install instruction</Button>
              <Button href="/instructions/" variant="secondary"><Icon name="arrow-left" size={15} /> Back to instructions</Button>
            </div>
          </header>

          <section class="content-panel pixel-panel" aria-labelledby="overview-title">
            <p class="panel-kicker">Overview</p>
            <h2 id="overview-title">What this instruction standardizes</h2>
            <p id="instruction-overview">Loading instruction overview.</p>
            <div class="feature-grid" aria-label="Instruction scope">
              <article><Icon name="document" size={18} /><h3>Apply to</h3><p id="instruction-apply-to">loading</p></article>
              <article><Icon name="code" size={18} /><h3>Extensions</h3><p id="instruction-extensions">loading</p></article>
              <article><Icon name="tools" size={18} /><h3>Patterns</h3><p id="instruction-patterns">loading</p></article>
            </div>
          </section>

          <section class="content-panel pixel-panel" aria-labelledby="usage-title">
            <p class="panel-kicker">Usage</p>
            <h2 id="usage-title">Install and run from your workspace</h2>
            <div class="terminal-card">
              <div class="terminal-card__bar"><span class="detail-badge detail-badge--neutral">npx</span><div class="terminal-card__actions"><button class="copy-button" type="button" data-copy-code aria-label="Copy install command"><Icon name="copy" size={14} /><span>Copy command</span></button></div></div>
              <pre><code data-install-command>npx awesome-copilot add instructions/loading.instructions.md</code></pre>
            </div>
            <p class="helper-copy">Use this command from your project root, then apply the instruction from your Copilot setup.</p>
          </section>

          <section class="content-panel pixel-panel" aria-labelledby="file-title">
            <div class="file-header"><p class="panel-kicker" id="instruction-path">instructions/loading.instructions.md</p><span>readonly</span></div>
            <h2 id="file-title">Instruction file metadata</h2>
            <dl class="spec-table">
              <div><dt>name</dt><dd id="spec-name">loading</dd></div>
              <div><dt>applyTo</dt><dd id="spec-apply-to">loading</dd></div>
              <div><dt>extensions</dt><dd id="spec-extensions">loading</dd></div>
              <div><dt>updated</dt><dd id="spec-updated">loading</dd></div>
            </dl>
          </section>
        </main>

        <aside class="detail-sidebar" aria-label="Instruction actions and metadata">
          <section class="sidebar-card pixel-panel" aria-labelledby="source-title">
            <p class="panel-kicker">Source</p><h2 id="source-title">Repository</h2><p class="source-owner"><Icon name="github" size={18} /> awesome-copilot</p>
            <Button href="/instructions/" variant="secondary" size="sm"><Icon name="open" size={14} /> View source listing</Button>
          </section>

          <section id="install" class="sidebar-card pixel-panel" aria-labelledby="install-title">
            <p class="panel-kicker">Install command</p><h2 id="install-title">Add this instruction</h2>
            <div class="terminal-card terminal-card--compact">
              <div class="terminal-card__bar"><span class="detail-badge detail-badge--neutral">npx</span><button class="copy-button" type="button" data-copy-code aria-label="Copy install command"><Icon name="copy" size={14} /><span>Copy command</span></button></div>
              <pre><code data-install-command>npx awesome-copilot add instructions/loading.instructions.md</code></pre>
            </div>
            <Button href="/instructions/" variant="primary" size="sm"><Icon name="download" size={14} /> Install from marketplace</Button>
          </section>

          <section class="sidebar-card pixel-panel" aria-labelledby="download-title">
            <p class="panel-kicker">Download</p><h2 id="download-title">Portable resource</h2>
            <Button href="/instructions/" variant="secondary" size="sm" ariaLabel="Download instruction file"><Icon name="archive" size={14} /> Download instruction file</Button>
            <Button href="/instruction/" variant="ghost" size="sm" ariaLabel="Share instruction"><Icon name="share" size={14} /> Share instruction</Button>
          </section>

          <section class="sidebar-card pixel-panel" aria-labelledby="useful-title">
            <p class="panel-kicker">Useful for</p><h2 id="useful-title">Best fit</h2><div class="tag-list" id="instruction-tags"></div>
          </section>

          <section class="sidebar-card pixel-panel" aria-labelledby="metadata-title">
            <p class="panel-kicker">Metadata</p><h2 id="metadata-title">Resource details</h2>
            <dl class="metadata-list"><div><dt>type</dt><dd>instruction</dd></div><div><dt>path</dt><dd id="metadata-path">loading</dd></div><div><dt>status</dt><dd id="metadata-status">loading</dd></div></dl>
          </section>
        </aside>
      </div>
    </Container>
  </section>
</BaseLayout>

<style>
  .agent-detail { padding-block: 1.75rem 3rem; }
  .breadcrumbs { display: flex; flex-wrap: wrap; align-items: center; gap: 0.55rem; margin-bottom: 1.25rem; color: var(--color-muted); font-size: 0.96rem; font-weight: 800; }
  .breadcrumbs a { color: var(--color-green); text-decoration: none; }
  .breadcrumbs a:hover { text-decoration: underline; text-underline-offset: 0.25em; }
  .breadcrumbs a:focus-visible { outline: 3px solid var(--color-green); outline-offset: 4px; }
  .detail-layout { display: grid; grid-template-columns: minmax(0, 1fr) minmax(19rem, 28rem); gap: clamp(1rem, 2vw, 1.5rem); align-items: start; }
  .detail-main, .detail-sidebar { display: grid; gap: 1.25rem; }
  .detail-sidebar { position: sticky; top: 1rem; }
  .pixel-panel { position: relative; overflow: hidden; border: 4px solid var(--color-border-soft); border-radius: 0; background: linear-gradient(180deg, rgb(255 255 255 / 2%), transparent), var(--color-bg-elevated); box-shadow: 4px 4px 0 var(--color-black); }
  .hero-panel { display: grid; gap: 1.35rem; overflow: visible; padding: clamp(1.25rem, 3vw, 2rem); border-top: 4px solid var(--color-subtle); border-bottom: 4px solid var(--color-subtle); border-left: none; border-right: none; }
  .hero-panel::before { content: ""; position: absolute; inset: 0; border-left: 4px solid var(--color-subtle); border-right: 4px solid var(--color-subtle); margin-left: -4px; margin-right: -4px; pointer-events: none; }
  .terminal-line, .panel-kicker { margin: 0; color: var(--color-green); font-family: var(--font-mono); font-size: 0.9rem; font-weight: 900; }
  .terminal-line::before { content: '> '; }
  .title-stack { display: grid; gap: 0.8rem; max-width: 52rem; }
  h1, h2, h3 { margin: 0; font-family: var(--font-heading); color: var(--color-text); }
  h1 { font-size: clamp(2.2rem, 5vw, 4.15rem); letter-spacing: -0.055em; line-height: 0.98; }
  h2 { font-size: clamp(1.35rem, 2vw, 1.75rem); letter-spacing: -0.035em; }
  h3 { font-size: 1.08rem; letter-spacing: -0.02em; }
  .title-stack p, .content-panel > p, .sidebar-card > p:not(.panel-kicker), .helper-copy { margin: 0; color: var(--color-muted); font-size: 1.02rem; line-height: 1.65; }
  .hero-actions { display: flex; flex-wrap: wrap; gap: 0.75rem; }
  .content-panel, .sidebar-card { display: grid; gap: 1rem; padding: 1.15rem; }
  .detail-badge { display: inline-flex; width: fit-content; align-items: center; border: 2px solid currentColor; border-radius: 0; box-shadow: 2px 2px 0 var(--color-black); font-family: var(--font-mono); font-size: 0.78rem; font-weight: 900; letter-spacing: 0.03em; line-height: 1; padding: 0.35rem 0.5rem; text-transform: uppercase; }
  .detail-badge--neutral { color: var(--color-muted); background: var(--color-surface-2); }
  .detail-badge--purple { color: var(--color-primary); background: rgb(169 112 255 / 12%); }
  .detail-badge--blue { color: var(--color-blue); background: rgb(92 211 255 / 12%); }
  .detail-badge--green { color: var(--color-green); background: rgb(144 255 177 / 12%); }
  .detail-badge--yellow { color: var(--color-yellow); background: rgb(255 214 102 / 12%); }
  .feature-grid { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 0.85rem; }
  .feature-grid article { display: grid; align-content: start; gap: 0.45rem; border: 2px solid var(--color-border-soft); background: var(--color-bg); box-shadow: 2px 2px 0 var(--color-black); padding: 1rem; }
  .feature-grid article :global(.icon) { color: var(--color-green); }
  .feature-grid p { margin: 0; color: var(--color-muted); font-size: 0.96rem; line-height: 1.5; overflow-wrap: anywhere; }
  .terminal-card { border: 2px solid var(--color-border-soft); background: var(--color-bg); }
  .terminal-card--compact { min-width: 0; }
  .terminal-card__bar, .file-header { display: flex; justify-content: space-between; gap: 1rem; align-items: center; border-bottom: 2px solid var(--color-border-soft); padding: 0.75rem 0.9rem; color: var(--color-muted); font-family: var(--font-mono); font-size: 0.9rem; font-weight: 800; }
  .terminal-card__actions { display: flex; flex-wrap: wrap; justify-content: end; gap: 0.55rem; align-items: center; }
  .copy-button { display: inline-flex; align-items: center; gap: 0.4rem; border: 1px solid var(--color-border-soft); border-radius: 0; background: var(--color-surface-2); color: var(--color-muted); cursor: pointer; font-family: var(--font-mono); font-size: 0.78rem; font-weight: 800; padding: 0.35rem 0.55rem; }
  .copy-button:hover, .copy-button[data-copied="true"] { border-color: var(--color-green); color: var(--color-green); }
  .copy-button :global(.icon) { font-size: 1.4rem; line-height: 1; }
  pre { overflow-x: auto; margin: 0; background: var(--color-black); color: var(--color-text); padding: 1rem; font-family: var(--font-mono); font-size: 0.98rem; line-height: 1.55; }
  .spec-table, .metadata-list { display: grid; margin: 0; border: 2px solid var(--color-border-soft); }
  .spec-table div, .metadata-list div { display: grid; grid-template-columns: minmax(7rem, 0.28fr) 1fr; border-bottom: 2px solid var(--color-border-soft); }
  .spec-table div:last-child, .metadata-list div:last-child { border-bottom: 0; }
  dt, dd { padding: 0.9rem; }
  dt { color: var(--color-primary); background: rgb(169 112 255 / 10%); font-family: var(--font-mono); font-weight: 900; text-transform: lowercase; }
  dd { margin: 0; color: var(--color-text); font-size: 0.98rem; overflow-wrap: anywhere; }
  .sidebar-card { border-width: 3px; box-shadow: 3px 3px 0 var(--color-black); }
  .sidebar-card h2 { font-size: 1.16rem; }
  .sidebar-card pre { padding: 0.9rem; font-size: 0.9rem; }
  .sidebar-card :global(.button) { width: 100%; justify-self: stretch; }
  .source-owner { display: flex; align-items: center; gap: 0.5rem; }
  .source-owner :global(.icon) { color: var(--color-text); }
  .tag-list { display: flex; flex-wrap: wrap; gap: 0.55rem; }
  @media (max-width: 980px) { .detail-layout { grid-template-columns: 1fr; } .detail-sidebar { position: static; } }
  @media (max-width: 700px) { .feature-grid { grid-template-columns: 1fr; } .spec-table div, .metadata-list div { grid-template-columns: 1fr; } dt { padding-bottom: 0.2rem; } dd { padding-top: 0.2rem; } }
</style>

<script>
  import { adaptInstruction } from '../lib/adapters/instructions';
  import type { InstructionItem, InstructionsData } from '../lib/upstream-types';

  const params = new URLSearchParams(window.location.search);
  const slug = params.get('slug');

  function setText(id: string, value: string): void {
    const element = document.getElementById(id);
    if (element) element.textContent = value;
  }

  function setHref(label: string, href: string): void {
    const link = document.querySelector<HTMLAnchorElement>(`a[aria-label="${label}"]`);
    if (link) link.href = href;
  }

  function joinOrNone(values: string[] | undefined): string {
    return values?.length ? values.join(', ') : '(none)';
  }

  function resolveApplyTo(instruction: InstructionItem): string {
    if (instruction.applyTo) return instruction.applyTo;
    if (instruction.applyToPatterns?.length) return instruction.applyToPatterns.join(', ');
    return '**';
  }

  function renderTags(tags: string[]): void {
    const tagList = document.getElementById('instruction-tags');
    if (!tagList) return;
    tagList.textContent = '';
    for (const tag of tags) {
      const badge = document.createElement('span');
      badge.className = 'detail-badge detail-badge--neutral';
      badge.textContent = tag;
      tagList.append(badge);
    }
  }

  function renderInstruction(rawInstruction: InstructionItem): void {
    const instruction = adaptInstruction(rawInstruction);
    const instructionPath = rawInstruction.path;
    const installCommand = `npx awesome-copilot add ${instructionPath}`;
    const sourcePath = `awesome-copilot/${instructionPath}`;
    const applyTo = resolveApplyTo(rawInstruction);
    const extensions = joinOrNone(rawInstruction.extensions);
    const patterns = joinOrNone(rawInstruction.applyToPatterns);
    const updated = rawInstruction.lastUpdated || '(unknown)';

    document.title = `${instruction.title} · Instruction · Awesome Copilot`;
    document.querySelector('meta[name="description"]')?.setAttribute('content', instruction.description);

    setText('breadcrumb-current', instruction.title);
    setText('instruction-terminal-line', `cat ./${instructionPath}`);
    setText('instruction-title', instruction.title);
    setText('instruction-description', instruction.description);
    setText('instruction-overview', instruction.description);
    setText('instruction-apply-to', applyTo);
    setText('instruction-extensions', extensions);
    setText('instruction-patterns', patterns);
    setText('instruction-path', instructionPath);
    setText('spec-name', instruction.title);
    setText('spec-apply-to', applyTo);
    setText('spec-extensions', extensions);
    setText('spec-updated', updated);
    setText('metadata-path', sourcePath);
    setText('metadata-status', instruction.label);

    const label = document.getElementById('instruction-label');
    if (label) {
      label.textContent = instruction.label;
      label.className = `detail-badge detail-badge--${instruction.accent}`;
    }

    for (const code of document.querySelectorAll<HTMLElement>('[data-install-command]')) {
      code.textContent = installCommand;
    }

    setHref('Download instruction file', `/instructions/#file=${encodeURIComponent(instructionPath)}`);
    setHref('Share instruction', `/instruction/?slug=${encodeURIComponent(instruction.slug)}`);
    renderTags(instruction.tags);
  }

  function renderError(message: string): void {
    setText('breadcrumb-current', 'Instruction not found');
    setText('instruction-title', 'Instruction not found');
    setText('instruction-description', message);
    setText('instruction-overview', message);
  }

  async function loadInstruction(): Promise<void> {
    if (!slug) {
      renderError('Missing instruction slug. Go back to the instructions listing and choose an instruction.');
      return;
    }

    try {
      const response = await fetch(`${import.meta.env.BASE_URL}data/instructions.json`);
      if (!response.ok) throw new Error(`Failed to load instructions data (${response.status})`);
      const data = (await response.json()) as InstructionsData;
      const rawInstruction = data.items.find((item) => item.id === slug);
      if (!rawInstruction) {
        renderError(`No instruction found for slug "${slug}".`);
        return;
      }
      renderInstruction(rawInstruction);
    } catch (error) {
      renderError(error instanceof Error ? error.message : 'Failed to load instruction details.');
    }
  }

  document.querySelectorAll<HTMLButtonElement>('[data-copy-code]').forEach((button) => {
    button.addEventListener('click', async () => {
      const code = button.closest('.terminal-card')?.querySelector('code')?.textContent?.trim();
      if (!code) return;
      const label = button.querySelector('span');
      const previousLabel = label?.textContent ?? 'Copy';
      try {
        await navigator.clipboard.writeText(code);
        button.dataset.copied = 'true';
        if (label) label.textContent = 'Copied';
        window.setTimeout(() => {
          button.dataset.copied = 'false';
          if (label) label.textContent = previousLabel;
        }, 1600);
      } catch {
        if (label) label.textContent = 'Failed';
        window.setTimeout(() => {
          if (label) label.textContent = previousLabel;
        }, 1600);
      }
    });
  });

  loadInstruction();
</script>
