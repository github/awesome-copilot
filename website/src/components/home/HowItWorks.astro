---
import Icon from '../Icon.astro';

const base = import.meta.env.BASE_URL;

const steps = [
  {
    title: 'Find',
    description: 'Search across every resource by technology, task, or workflow.',
    icon: 'search' as const,
  },
  {
    title: 'Install',
    description: 'Add a plugin with one command, or drop instructions into your repo.',
    icon: 'download' as const,
  },
  {
    title: 'Ship',
    description: 'Invoke agents and skills so Copilot follows your project standards.',
    icon: 'external' as const,
  },
];
---

<section class="how-it-works" aria-labelledby="how-it-works-heading">
  <div class="container">
    <div class="how-it-works-header reveal">
      <h2 id="how-it-works-heading">How it works</h2>
      <p>
        Get from discovery to delivery in three steps. Everything in the collection is
        designed to be copied, installed, or invoked without heavy setup.
      </p>
    </div>

    <div class="commit-history reveal-stagger" role="list">
      <div class="commit-history__line" aria-hidden="true"></div>
      {
        steps.map((step, index) => (
          <article class="commit-entry reveal" style={`--animation-delay: ${index * 120}ms;`} role="listitem">
            <div class="commit-entry__avatar" aria-hidden="true">
              <span class="commit-entry__dot"></span>
              <span class="commit-entry__icon">
                <Icon name={step.icon} size={18} />
              </span>
            </div>
            <div class="commit-entry__body glass-card">
              <div class="commit-entry__header">
                <h3>{step.title}</h3>
                <span class="commit-entry__hash" aria-hidden="true">{String(index + 1).padStart(2, '0')}</span>
              </div>
              <p>{step.description}</p>
            </div>
          </article>
        ))
      }
    </div>

    <div class="how-it-works-footer reveal">
      <a href={`${base}how-to-use/`} class="btn btn-secondary">
        Read the full guide
        <Icon name="external" size={16} />
      </a>
    </div>
  </div>
</section>
