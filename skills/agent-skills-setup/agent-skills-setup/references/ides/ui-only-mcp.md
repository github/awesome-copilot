# UI-managed MCP clients

Use for iFlyCode (`iflycode`) and the ambiguous Raccoon AI (`raccoon-ai`) name.
No current official, versioned file or UI MCP contract is recorded for either
entry. They are unverified references, not supported migration targets. Bind an
exact vendor product URL and version before adding any profile; until then, do
not invent a path, JSON root key, or portable context surface.
