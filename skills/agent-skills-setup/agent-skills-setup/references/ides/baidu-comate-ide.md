# baidu-comate-ide (standalone IDE)

**Registry status:** `unverified/manual-reference`; it is not an automatic
source or target. MCP is UI-managed; rules, custom agents, commands, memory,
Spec/Mission, and user state require a versioned official profile. This
standalone IDE is distinct from the Comate plugin; do not reuse plugin paths.
