# jules

Google Jules is an asynchronous coding agent providing cloud, CLI, and API profiles. It reads root `AGENTS.md`, repository setup scripts, and environment snapshots. Automated migrations treat Jules as a cloud-rebuild profile.

Sources: [docs](https://jules.google.com/).
