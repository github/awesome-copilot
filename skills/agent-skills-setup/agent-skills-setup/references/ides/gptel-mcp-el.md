# gptel-mcp-el

`mcp.el`/gptel is package-specific Emacs Lisp, not native Emacs MCP. Install, init snippets, server registration, and package state require manual review; do not treat `~/.emacs.d/` as a Skills directory.
