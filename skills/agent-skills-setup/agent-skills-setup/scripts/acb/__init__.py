"""Agent Context Bundle (ACB) support modules.

An ACB is a portable, cross-device snapshot of agent context.  It is
intentionally separated from the migration plan document so that a
plan (which is bound to a single device's absolute paths) can be
produced from an ACB on a different device.
"""